name: Reset Appreciation Lock
  
permissions:
  issues: write
  
on:
  schedule:
    - cron: '0 0 * * *'
  workflow_dispatch:
    
jobs:
  reset:
    runs-on: ubuntu-latest
    
    steps:
      - name: Remove label commit-today
        run: |
          ISSUE_NUMBER=1
          REPO="fridfn/workflow-development"

          curl -s -X DELETE \
            -H "Authorization: token ${{ secrets.ACTIVITY_GITHUB_TOKEN }}" \
            https://api.github.com/repos/$REPO/issues/$ISSUE_NUMBER/labels/commit-today
            
          if [ "$response" = "200" ] || [ "$response" = "204" ]; then
            echo "Label removed"
          else
            echo "Label not found, skip"
          fi